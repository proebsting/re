RX TO-DO LIST

WEB APP:
Change name?
Write intro page and other explanatory prose
Finish & finalize styling
Always add a blank row for another comparator example [up to 10 total]

REGEXPS:
Accept/ignore Perl comments (?#
Accept/ignore capture names (?<identifier>

--------------------------------------------------------------------

Probably not to be done:

REGEXPS:
Expand wildcards and classes to include Unicode
Add POSIX classes?
Add \b anchor? Embedded ^ and $?
Add flags such as "case insensitive"??
Other Perl stuff?

UTILITIES:
Handle newlines in exprs somehow?  
Add metadata to the JSON output of rxg
In rxcluster, subdivide large clusters
Show WHICH pattern accepts on DOT output

CLEANUP:
Add func for "canonizing" DFA (clearer output, easier testing)
Make gofmt pre-commit hook automatic in every Git clone
